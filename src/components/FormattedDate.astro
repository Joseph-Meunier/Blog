---
interface Props {
	date: Date;
	locale?: string;
}

const { date, locale = 'fr-FR' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale, {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
